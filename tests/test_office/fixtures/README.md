Office test fixtures
====================

The office test suite primarily uses dynamically generated files and mocks.

This directory exists so the suite has a stable fixtures location when tests
need to reference on-disk sample assets.
